% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other_utils.R
\name{ody_write_xlsx}
\alias{ody_write_xlsx}
\title{Write Multiple Data Frames to an Excel Workbook}
\usage{
ody_write_xlsx(..., .file_path, .add_version = TRUE)
}
\arguments{
\item{...}{Data frames to be written to the Excel file. Each argument will
become a separate worksheet, with the worksheet name derived from the
argument name or expression.}

\item{.file_path}{Character string specifying the file path where the Excel
file should be saved.}

\item{.add_version}{Logical. If \code{TRUE} (default), uses \code{ody_save_path()} to
save the file with version control. If \code{FALSE}, uses \code{here::here()} for
the file path.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its side effect
of creating an Excel file.
}
\description{
This function creates an Excel workbook with multiple worksheets, where each
worksheet contains a data frame. The worksheets are automatically formatted
with tables and auto-sized columns.
}
\details{
The function performs the following operations:
\itemize{
\item Creates a new Excel workbook using \code{openxlsx2}
\item Adds each data frame as a separate worksheet
\item Formats data as tables with empty strings for NA values
\item Auto-sizes column widths for readability
\item Saves the workbook to the specified path
}
}
\examples{
\dontrun{
# Write multiple data frames to Excel
ody_write_xlsx(
  iris_data = iris,
  mtcars_data = mtcars,
  .file_path = "output/my_data.xlsx"
)

# Write without version control
ody_write_xlsx(
  sales = sales_df,
  .file_path = "reports/sales.xlsx",
  .add_version = FALSE
)
}

}
\seealso{
\code{\link[openxlsx2:wb_workbook]{openxlsx2::wb_workbook()}}, \code{\link[=ody_save_path]{ody_save_path()}}
}
