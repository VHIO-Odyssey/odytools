% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomization.R
\name{ody_make_random_list}
\alias{ody_make_random_list}
\title{Build Stratified Block Randomization Lists}
\usage{
ody_make_random_list(
  n,
  rnd_levels,
  rnd_name = NULL,
  rnd_strata_list = NULL,
  block_size
)
}
\arguments{
\item{n}{An integer scalar specifying the number of allocations to generate
per stratum.}

\item{rnd_levels}{A character vector specifying the treatment arm labels
passed to \code{levels} in \code{\link[blockrand:blockrand]{blockrand::blockrand()}}.}

\item{rnd_name}{A character scalar specifying the output column name for the
assigned treatment. If \code{NULL} (default), the column is named
\code{"treatment"}.}

\item{rnd_strata_list}{A named list of vectors defining stratification
factors. Each element name becomes a column in the output and each vector
provides the allowed levels for that factor. If \code{NULL} (default), no
stratification is applied and a single randomization list is generated.}

\item{block_size}{An integer vector specifying the number of times each
treatment level is represented within a single block. The resulting total
block size is \code{block_size * length(rnd_levels)}. For example, with two
arms (\code{A} and \code{B}), \code{block_size = 1} produces blocks of size 2 (1 A,
1 B), and \code{block_size = 2} produces blocks of size 4 (2 A, 2 B). With
three arms (\code{A}, \code{B}, \code{C}), \code{block_size = 1} produces blocks of size 3
(1 A, 1 B, 1 C). When a vector is supplied (e.g., \code{block_size = c(1, 2)}),
each block is randomly assigned one of the allowed sizes. This argument
is passed as \code{block.sizes} to \code{\link[blockrand:blockrand]{blockrand::blockrand()}}.}
}
\value{
A tibble with one row per randomized allocation across all strata.
Includes \code{block_id} and \code{block_size}, optional stratification columns from
\code{rnd_strata_list}, and a treatment-assignment column (\code{treatment} by
default, or \code{rnd_name} when provided).

The \code{block_id} values are character identifiers constructed by
concatenating stratum values (if present) and the per-stratum block id.
}
\description{
Generates treatment allocation lists using permuted blocks, optionally
stratified by one or more factors.

The function expands all combinations in \code{rnd_strata_list}, generates a
separate block-randomized sequence for each stratum with
\code{\link[blockrand:blockrand]{blockrand::blockrand()}}, and returns a single tibble combining all
strata-specific randomization schedules.
}
\details{
This function requires the \code{blockrand} package at runtime and checks for
its availability with \code{\link[rlang:check_installed]{rlang::check_installed()}}.
}
\examples{
ody_make_random_list(
  n = 12,
  rnd_levels = c("A", "B"),
  rnd_name = "arm",
  rnd_strata_list = list(site = c("S1", "S2"), sex = c("F", "M")),
  block_size = c(2, 4)
)

}
