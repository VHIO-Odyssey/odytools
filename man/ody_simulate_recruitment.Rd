% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomization.R
\name{ody_simulate_recruitment}
\alias{ody_simulate_recruitment}
\title{Simulate Recruitment from a Stratified Randomization List}
\usage{
ody_simulate_recruitment(rnd_list, n_recruitment)
}
\arguments{
\item{rnd_list}{A stratified tibble produced by \code{\link[=ody_make_random_list]{ody_make_random_list()}} with
a non-\code{NULL} \code{rnd_strata_list} argument. Must contain a \code{stratum} column
and carry the \code{"rnd_name"} attribute identifying the treatment column.}

\item{n_recruitment}{A single positive integer specifying the total number of
subjects to simulate.}
}
\value{
A named list with three elements:
\describe{
\item{\code{simulated_recruitment}}{A tibble of the allocated rows from
\code{rnd_list}, one row per simulated subject, preserving all original
columns.}
\item{\code{final_balance}}{A tibble with one row per treatment arm and a
column \code{n} with the total number of allocations to that arm across all
strata.}
\item{\code{inbalance}}{A single integer equal to
\code{max(n) - min(n)} across treatment arms in \code{final_balance}, measuring
the magnitude of treatment imbalance at the end of recruitment.}
}
}
\description{
Simulates patient recruitment by randomly assigning strata to a given
number of subjects and extracting their corresponding treatment allocations
from a pre-built stratified randomization list.

For each simulated recruit, a stratum is drawn uniformly at random (with
replacement) from the unique strata in \code{rnd_list}. Subjects within each
stratum are then consumed sequentially from the top of the list. The
function reports the resulting treatment balance and the magnitude of
imbalance across arms at the end of recruitment.
}
\seealso{
\code{\link[=ody_make_random_list]{ody_make_random_list()}}
}
