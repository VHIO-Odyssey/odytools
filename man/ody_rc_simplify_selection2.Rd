% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap.R
\name{ody_rc_simplify_selection2}
\alias{ody_rc_simplify_selection2}
\title{Simplify a selection by dropping RedCap structure variables (conservative)}
\usage{
ody_rc_simplify_selection2(selected_data, join = FALSE)
}
\arguments{
\item{selected_data}{A data frame or list of data frames (typically the
output of \code{ody_rc_select()} or \code{ody_rc_select_form()}).}

\item{join}{Logical. If TRUE and \code{selected_data} is a list of data frames,
the simplified data frames will be joined with \code{dplyr::full_join} by all
the common columns. Defaults to \code{FALSE}: joining forms coming from
different events/repeating structures can create unexpected many-to-many
cross joins, so it is left to the user to decide, on a case by case basis,
whether \code{join = TRUE} is appropriate.}
}
\value{
A simplified data frame or list of data frames without the
RedCap structural columns that can be safely removed.
}
\description{
Remove RedCap structural columns (\code{redcap_form_name},
\code{redcap_instance_type}, and, when safe, \code{redcap_event_name} and
\code{redcap_instance_number}) from a data frame or list of data frames.
}
\details{
This function only relies on information that is stable and does not
depend on how much data has been collected so far:
\itemize{
\item \code{redcap_form_name} and \code{redcap_instance_type} are always removed, since
they never carry information useful for downstream analysis once a
selection has been made.
\item \code{redcap_event_name} is removed only if the form belongs to a single
possible event, according to \code{forms_events_mapping} (project design
metadata, unaffected by REDCap API privilege restrictions).
\item \code{redcap_instance_number} is removed only if the form belongs to a single
possible event \strong{and} the already selected data has at least one row
(to rule out the vacuous case of no data at all) with
\code{redcap_instance_number} equal to \code{NA} in every row. If the form appears in
more than one event,
or if there is no data at all yet, \code{redcap_instance_number} is
never removed.
}

Because of these rules, the output of this function is stable across
successive imports of the same project as more data is collected: a
column is only dropped when it can be shown, from stable metadata or from
data actually observed, that it carries no information yet.

This function supersedes \code{ody_rc_simplify_selection()}, which relied on
the \code{repeating} attribute inferred from the data (now the only option
since \code{repeatingFormsEvents} is no longer exportable with standard API
privileges) and could therefore give a different (and potentially unsafe)
answer for the same project depending on when it was imported or which
data access groups the importing user could see.
}
